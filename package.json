{
  "name": "vendor-guardian",
  "private": true,
  "packageManager": "npm@9.2.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run build --workspaces --if-present && (cd packages/vendor-client && node --test --test-concurrency=1 dist/core/*.test.js dist/expectations/*.test.js dist/vendors/*.test.js) && (cd apps/worker && node --test --test-concurrency=1 dist/jobs/*.test.js)",
    "report": "tsx scripts/report.ts",
    "start:standalone": "npm run start --workspace @roco/vendor-guardian-standalone"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/pg": "^8.11.10",
    "pg": "^8.13.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
